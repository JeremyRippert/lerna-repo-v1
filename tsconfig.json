// tsconfig.json
{
  "compilerOptions": {
    "baseUrl": ".",

    // are those needed?
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,

    "strict": true,

    "skipLibCheck": true,

    // should probable change
    "jsx": "react",

    "esModuleInterop": true,
    "module": "commonjs",
    "moduleResolution": "node",
    "target": "es2015",

    // required for monorepo
    "composite": true
  },
  // required for monorepo?
  "references": [
    { "path": "./packages/shared/tsconfig.json" }
    // Commented because otherwise we get error
    // Referenced project '/home/jeremy/dev/jeremy/templates/lerna-repo/packages/frontend/tsconfig.json' may not disable emit.
    // { "path": "./packages/frontend/tsconfig.json" }
  ]
}
